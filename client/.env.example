# Browser API base URL. Leave empty in local dev to use Vite proxy.
# VITE_API_BASE_URL=https://your-backend-domain.com

# Dev-only Vite proxy target for /api/* requests.
VITE_DEV_API_PROXY_TARGET=http://localhost:8080
